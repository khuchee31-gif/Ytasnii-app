name: protocol
description: >-
  «Хот унтлаа» онлайн — сервер ба апп хоёрын хуваалцах мессежийн хэл.
  Цэвэр Dart: dart:io БАЙХГҮЙ, Flutter БАЙХГҮЙ. Хоёр тал ижил файл уншина.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
