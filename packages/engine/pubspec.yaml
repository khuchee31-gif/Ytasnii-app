name: engine
description: «Хот унтлаа» — шөнийн үйлдэл шийдвэрлэх цэвэр хөдөлгүүр. Flutter-гүй, dart:io-гүй, IO-гүй.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
